# Jaddati — configuration template.
# The iOS app reads its key from Jaddati/Secrets.plist (git-ignored), not from
# this file. This exists so the required variable is documented in the repo
# without the value ever being committed.

ELEVENLABS_API_KEY=sk_your_key_here
